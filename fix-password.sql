-- ====================================================
-- 修复密码字段长度问题
-- ====================================================
-- BCrypt 加密后的密码长度为 60 字符，需要先修改数据库表结构
ALTER TABLE users MODIFY COLUMN password VARCHAR(60) NOT NULL;

-- ====================================================
-- 方案一：清空用户表，让系统重新初始化（会删除所有用户数据！）
-- ====================================================
-- 如果数据库中有重要数据，请先备份！

DELETE FROM users;

-- 重启后端后，DataInitializer 会自动创建以下账号（密码已 BCrypt 加密）：
-- admin / 123456
-- parent1 / 123456
