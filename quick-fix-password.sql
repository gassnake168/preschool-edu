-- ====================================================
-- 快速修复方案：仅修改密码字段长度
-- ====================================================
-- 如果你不想删除现有用户数据，只运行这一条语句即可

ALTER TABLE users MODIFY COLUMN password VARCHAR(60) NOT NULL;

-- 执行完后，删除 PasswordEncryptor.java 文件，
-- 然后重启后端，密码会自动加密
